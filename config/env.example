# Exemplo de configuração (.env)
# Caminho absoluto do frida-server correspondente ao frida --version do host
FRIDA_SERVER_BIN="$HOME/tools/frida/frida-server-17.2.16-android-x86_64"
# Package padrão para testes
PKG_DEFAULT="com.android.settings"
# Dispositivo (emulador padrão)
DEVICE_ID="emulator-5554"
